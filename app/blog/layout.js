export const metadata = {
  title: 'Animal Welfare Field Notes and Rescue Updates',
  description: 'Read LAHIT Animal Welfare updates about rescue work, medical care, feeding drives, adoption, and volunteers across Uttarakhand.',
  alternates: { canonical: '/blog/' },
};

export default function BlogLayout({ children }) {
  return children;
}
